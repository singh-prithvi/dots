# Asus battery capping aliases
alias bat60 "sudo asusctl battery limit 60"
alias bat80 "sudo asusctl battery limit 80"
alias bat100 "sudo asusctl battery limit 100"
alias batcheck "asusctl battery info"
